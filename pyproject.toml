[project]
name = "plantwide"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas",
    "numpy",
    "scikit-learn",
    "xgboost",
    "python-pptx",
    "openpyxl",
    "matplotlib",
    "jupyter",
]
